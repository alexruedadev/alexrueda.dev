/* 
beforeEach(() => {
    // do before each test.
});

afterEach(() => {
    // do after each test.
});
*/
describe('a group of tests',() => {
      
    it('first test', () => {
        expect(0).toBe(0);
    });

});
